version: '3.8'
services:
  yt-feeds-proxy:
    build: .
    image: yt-feeds-proxy:latest
    container_name: yt-feeds-proxy
    ports:
      - "8080:8080"
    volumes:
      - ./config.json:/app/config.json:ro
    restart: unless-stopped
